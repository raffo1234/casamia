export const getAdminPropertiesUserKey = (userId: string) => {
  return `admin-properties-${userId}`;
};

export const inputClassName =
  "disabled:bg-slate-50 w-full px-3 py-2.5 rounded-xl border border-gray-200 focus:outline-none focus-within:ring-4 focus-within:ring-cyan-100 focus-within:border-cyan-500";
